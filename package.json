{
  "name": "soynativo-sergio",
  "version": "0.1.0",
  "private": true,
  "description": "SOY NATIVO landing page",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o public/styles.css --minify",
    "watch": "tailwindcss -i src/input.css -o public/styles.css --watch",
    "serve": "python3 -m http.server 8765 --directory public",
    "dev": "npm run watch & npm run serve"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:33101/git/vasylievmarko-debug/soynativo-sergio"
  },
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "tailwindcss": "^4.2.4"
  }
}
